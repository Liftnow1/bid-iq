__pycache__/
*.pyc
*.egg-info/
dist/
build/
.env
.env.local
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Dedup artifacts (local-only, not source of truth)
**/_DUPLICATES/
**/_DEDUP-REPORT.csv
**/_DEDUP-EXECUTION-LOG.csv
data/product_data/_DEDUP-REPORT-ALL.csv
data/product_data/_DEDUP-EXECUTION-LOG-ALL.csv

# Ingest logs (local-only run artifacts)
logs/
data/extraction-errors.log

# Daily watcher state (local-only; per-machine state)
data/.ingest-manifest.json
data/.ingest-log/

# Throwaway n8n maintenance scripts. They reference live n8n JWT + agent-secret
# values inline — never commit them. The audit JSONs they produce are tracked
# under audit/data/workflows/ which IS committed.
.tmp_n8n/
